//- doctype html 
//- html 
//-  head 
//-   title Simple Web App 
//-  body 
extends layout 
block content
  ul.list-group
    if articles && articles.length
      each article, i in articles
        li.list-group-item
          a(href="/article/" + article._id)= article.title
    else
      li.list-group-item No articles found.